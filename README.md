This program is intended to convert a variable width, flat CSV file to a hierarchical JSON file. 

Can be run from the Command Line Interface by using
```
dotnet run [path to input file] [path to output directory]
```